# 이 파일을 .env.local 로 복사해 사용하세요.

# 클라이언트(브라우저)용 — route.ts 프록시 베이스 (같은 출처)
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# 서버 전용 — FastAPI 백엔드 주소 (NEXT_PUBLIC_ 아님 → 브라우저 비노출)
BACKEND_URL=http://localhost:8000
